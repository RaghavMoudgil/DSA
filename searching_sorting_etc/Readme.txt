Simple patterns to find the complexity of the certain program

Single Loop: O(n)

Nested Loops: O(n^2)

Sorting: Quicksort/Mergesort O(n log n), Bubble/Selection/Insertion O(n^2)

Searching: Linear O(n), Binary O(log n).


Frequently Asked Interview Questions
How do you reverse an array/slice in Go?

How would you sort a slice of custom structs? Implement sort.Interface.

Find the time complexity of Go's built-in functions (like len, append).

What’s a stable sort vs unstable? Which sorting algorithms are stable?

How do you handle concurrency when sorting/searching huge data in Go?

Implement binary search; discuss why it’s O(log n).
